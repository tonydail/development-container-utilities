services:
  code_container:
    extends:
      service: service_base
    image: mcr.microsoft.com/devcontainers/base:bullseye
    container_name: ${SERVICE_NAME}_code
    tty: true
    command: sh
    volumes:
      - sync_data:${SYNC_SERVER_WORKING_PATH}:rw
  code_sync:
    extends:
      service: service_base
    image: debian:bullseye-slim
    container_name: ${SERVICE_NAME}_sync
    tty: true
    volumes:
      - sync_data:${SYNC_SERVER_WORKING_PATH}:rwo
  service_base:
    image: mcr.microsoft.com/devcontainers/base:bullseye
    env_file:
      - .env
    networks:
      - devcontainer_network_alias
networks:
  devcontainer_network_alias:
    name: ${SERVICE_NAME}_network
    driver: bridge
volumes:
  sync_data:
  unison: 
